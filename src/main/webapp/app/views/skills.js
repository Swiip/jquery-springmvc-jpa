define([
  'bootstrap',
  'underscore',
  'backbone'
], function( $, _, Backbone ) {

  console.log("Skills");
  
  var SkillsView = Backbone.View.extend({
    
  });

  return SkillsView;

});
